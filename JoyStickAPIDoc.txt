  подключаем библиотеку #include "JoyStick.h"
  DevC++ нужно в параметрах проекта подключить libxinput_1.4.a
  в Visual Studio #pragma comment("xinput_1.4.lib");
  скачать xinput.h
  //
  JoyStickAPI* xController1; //Объявление контроллеров
  JoyStickAPI* xController2;

  xController1 = new JoyStickAPI(1);//присвоение номера контроллера
  xController2 = new JoyStickAPI(2);
  //Использование
  if(xController1->GetState().Gamepad.wButtons & XINPUT_GAMEPAD_A)
  {
  // код
  }
  //Аналоги
  xController1->GetState().Gamepad.sThumbLX // возвращяют значение оси левого стика по оси X
  xController1->GetState().Gamepad.sThumbLY // возвращяют значение оси левого стика по оси Y
  xController1->GetState().Gamepad.sThumbRX // возвращяют значение оси правого стика по оси X
  xController1->GetState().Gamepad.sThumbRY // возвращяют значение оси правого стика по оси Y
  //
  //Триггеры
  xController1->GetState().Gamepad.bLeftTrigger // возвращяет байтовое значение в диапазоне 0...255
  xController1->GetState().Gamepad.bRightTrigger // возвращяет байтовое значение в диапазоне 0...255
  //проверка контроллера
 if(xController1->IsConnected())
 {
  //Контроллер 1 подключен
 } 
 if(!xController1->IsConnected())
 {
  //Контроллер 1 не подключен
 } 
 //